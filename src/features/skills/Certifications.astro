---
import Carrousel from "@/components/Carrousel.astro";
import ImageModal from "@/components/modals/ImageModal.astro";
import { CERTIFICATIONS } from "@/data/certifications";
import CertificationItem from "@/features/skills/CertificationItem.astro";
import { useT } from "@/i18n/utils";

const t = useT();
---

<div class="w-full max-w-4xl mt-12">
	<!-- Cabecera con separador -->
	<div class="flex items-center gap-4 mb-6 px-2" data-aos="fade">
		<div class="h-px flex-1 bg-neon opacity-30"></div>
		<h2
			class="font-main text-[18px] sm:text-[20px] font-bold text-neon uppercase tracking-widest"
			data-translate="skills.certifications.title"
		>
			{t("skills.certifications.title")}
		</h2>
		<div class="h-px flex-1 bg-neon opacity-30"></div>
	</div>

	<Carrousel id="certifications" itemHalfWidth="9rem">
		{
			CERTIFICATIONS.map(({ image, title, issuer, date, verifyUrl }) => (
				<div class="snap-center shrink-0 w-[min(300px,_calc(100%-1rem))] sm:w-72">
					<CertificationItem
						image={image}
						title={title}
						issuer={issuer}
						date={date}
						verifyUrl={verifyUrl}
					/>
				</div>
			))
		}
	</Carrousel>
</div>

<ImageModal id="cert-modal" triggerAttr="data-cert-image" titleAttr="data-cert-title" />
